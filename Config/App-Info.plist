<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.utilities</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<key>NSHumanReadableCopyright</key>
	<string>WindowsM — experimental Windows 11 ARM64 installer for Apple Silicon</string>
	<!--
	  SMJobBless: code-signing requirement the embedded helper must satisfy.
	  The loose `identifier` form below works for local ad-hoc/dev builds.
	  For release, tighten it to e.g.:
	    identifier "com.windowsm.helper" and anchor apple generic and
	    certificate leaf[subject.OU] = "YOURTEAMID"
	-->
	<key>SMPrivilegedExecutables</key>
	<dict>
		<key>com.windowsm.helper</key>
		<string>identifier "com.windowsm.helper"</string>
	</dict>
	<!--
	  App Transport Security: the UUP download path fetches Windows update
	  packages from Microsoft's Windows Update delivery CDN, which serves over
	  plain HTTP (dl.delivery.mp.microsoft.com, *.download.windowsupdate.com,
	  and assorted regional mirrors whose hostnames vary per build/region).
	  ATS blocks HTTP by default, producing "requires the use of a secure
	  connection". The uupdump.net API itself is HTTPS; only the CDN payloads
	  are HTTP. We scope insecure loads to the known Microsoft delivery domains
	  and otherwise keep ATS on.
	-->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
		<key>NSExceptionDomains</key>
		<dict>
			<key>delivery.mp.microsoft.com</key>
			<dict>
				<key>NSIncludesSubdomains</key>
				<true/>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
			</dict>
			<key>download.windowsupdate.com</key>
			<dict>
				<key>NSIncludesSubdomains</key>
				<true/>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
			</dict>
			<key>windowsupdate.com</key>
			<dict>
				<key>NSIncludesSubdomains</key>
				<true/>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
